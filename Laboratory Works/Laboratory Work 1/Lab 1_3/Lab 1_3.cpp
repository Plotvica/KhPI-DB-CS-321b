#include <iostream>
#include <math.h>

int main()
{
    int x, a, b, c; 
        x = 2;
        a = x * x;
        b = a * a;
        c = b * b;
        x = c;

       // int answer = pow(x, 2) * pow(x, 2) * pow(x, 2) * pow(x, 2); 
            // y = pow (x, 8)          
}


