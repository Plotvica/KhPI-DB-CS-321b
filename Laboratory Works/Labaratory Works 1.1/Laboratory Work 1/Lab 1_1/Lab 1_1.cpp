#include <iostream>
//#include <Windows.h>

int main()
{
	//SetConsoleCP(1251);
	//SetConsoleOutputCP(1251);

	int іnt, shоrt, lоng, chаr, floаt, doublе;
	 іnt = sizeof(int);
	 shоrt = sizeof(short);
	 lоng = sizeof(long);
	 chаr = sizeof(char);
	 floаt = sizeof(float);
	 doublе = sizeof(double);
}


