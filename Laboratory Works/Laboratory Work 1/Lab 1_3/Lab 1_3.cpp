#include <iostream>
#include <math.h>

int main()
{
    int x, a, b, answer; 
        x = 2;
        a = x * x;
        b = a * a;
        answer = b * b;

       // int answer = pow(x, 2) * pow(x, 2) * pow(x, 2) * pow(x, 2); 
            // y = pow (x, 8)          
}


