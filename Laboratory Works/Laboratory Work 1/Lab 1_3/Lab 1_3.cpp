#include <iostream>
#include <math.h>

int main()
{
	int x, g, h, answer;
	x = 2;
	g = x * x;
	h = g * g;
	answer = h * h;

       // int answer = pow(x, 2) * pow(x, 2) * pow(x, 2) * pow(x, 2); 
            // y = pow (x, 8)          
}


