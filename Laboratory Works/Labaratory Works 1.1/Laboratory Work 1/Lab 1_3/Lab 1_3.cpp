#include <iostream>
#include <math.h>

int main()
{
	int x, y, answer;
	x = 2;
	y = x * x;
	y = y * y;
	answer = y * y;

       // int answer = pow(x, 2) * pow(x, 2) * pow(x, 2) * pow(x, 2); 
            // y = pow (x, 8)          
}


