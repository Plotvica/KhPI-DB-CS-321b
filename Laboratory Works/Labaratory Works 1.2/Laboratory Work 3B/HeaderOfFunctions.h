#pragma once
#include <iostream>
#include <iomanip>
#include "windows.h"

using namespace std;


void printArray(int* pointer, const int size);
void randomArray(int* pointer, const int size);
void task(int* A_pointer, int* B_pointer, int* C_pointer, int* D_pointer, int SIZE);